# 后续开发路线图

> 项目：面向中小型网络的自动化巡检与故障诊断平台
> 最后更新：2026-08-14（ABCD 全阶段完成）

---

## A 阶段：工程加固 ✅

**归档**：`docs/phase-a/archive.md`（提交 `915cd79`）

| 序号 | 项目 | 关键实现 |
|---|---|---|
| A1 | 登录安全加固 | token 过期（`api_token_expires_at`）、登录失败限流（内存计数，5次/15分钟）、密码策略（≥8位）、`POST /api/auth/change-password` |
| A2 | 巡检异步化 | `executor.py` 守护线程 + 内存队列；`run_task` 返回 pending；前端自动轮询（2s）；调度器改为入队 |
| A3 | 索引与分页 | 5 个组合索引（results/runs/alerts），`_ensure_sqlite_indexes` 幂等迁移 |
| A4 | MySQL 适配 + 配置外置 | `pymysql` + `pool_pre_ping`/`pool_recycle`；`.env.example`（45+ 变量）；注释 compose mysql |
| A5 | 审计日志 | `operation_logs` 表 + 全部写操作接入（登录/登出/资产/任务/告警/报告/发现）；前端 `audit.html` |
| A6 | 备份脚本 | `scripts/backup.sh` + `scripts/backup.ps1`（带时间戳 + 过期清理） |

测试：**49 passed** → 提交 `915cd79`

---

## B 阶段：业务补强 ✅

**归档**：`docs/phase-b/archive.md`（提交 `1884c50`）

| 序号 | 项目 | 关键实现 |
|---|---|---|
| B1 | 告警通知 | `notifications.py`：SMTP（SSL/STARTTLS）+ Webhook；`notification_min_level` 等级过滤；由 executor 调用 |
| B2 | 资产 CSV | `POST /import`（UTF-8，跳过重复 IP）+ `GET /export`（BOM）；前端按钮；`python-multipart` |
| B3 | 任务控制 | `cancel_requested` 字段 + 资产粒度取消；失败重试（`retry` 端点）；`schedule_cron` Cron 表达式 |
| B4 | 多用户角色 | `admin/operator/viewer` 三角色；`require_write` 拦截只读；`/api/users` 管理；`users.html` |
| B5 | TLS 检测器 | `TlsChecker`：SSL 握手 + 证书过期预警（30天）；`tls` 加入 `CHECK_TYPES` |
| B6 | 资产变更日志 | `asset_change_logs` 表 + 字段级 diff；`GET /assets/{id}/changes`；`asset-changes.html` |

测试：**71 passed** → 提交 `1884c50`

---

## C 阶段：真实网络能力 ✅

**归档**：`docs/phase-c/archive.md`（提交 `d3f0a24`）

| 序号 | 项目 | 关键实现 |
|---|---|---|
| C1 | 容器适配 | `verify-container-network.sh`；`frontend/Dockerfile` 改为 `COPY *.html`；排障文档 |
| C2 | Traceroute | `traceroute.py`（BSD/Linux 解析）+ `POST /api/diagnostics/traceroute`；`diag.html` 前端 |
| C3 | Nmap（可选） | `nmap_discovery.py`：`nmap -sn` 批量发现 + `nmap -sT -p --open` 端口；自动回落 |
| C4 | SNMP（可选） | `snmp_basic.py`：`snmpget`/`snmpwalk` 只读采集；`collect_device_basics` 快照；自动回落 |
| C5 | LAN 验证 | `scripts/verify-lan.sh`：逐项 ping/TCP/traceroute/HTTP/SNMP/Nmap |

测试：**88 passed** → 提交 `d3f0a24`

---

## D 阶段：进阶扩展 ✅

**归档**：`docs/phase-d/archive.md`（提交 `1a2a7fd`）

| 序号 | 项目 | 关键实现 |
|---|---|---|
| D1 | AI 诊断 | `ai_diagnosis.py`：OpenAI 兼容 chat/completions；`POST /api/diagnosis/{id}/ai-suggestion`；`ai_*` 配置 |
| D2 | Prometheus | `metrics.py`（零依赖）；`GET /metrics`；含 assets/tasks/runs/results/alerts/diagnoses 统计 |
| D3 | K8s 巡检 | `scripts/k8s-inspect.sh`：kubectl 采集节点/工作负载快照，输出 JSON |
| D4 | 分布式锁 | `TaskLock` 表 + `execute_lock.py`；`process_run` 加锁/释放；多实例防重复 |
| D5 | 有界队列 | `run_queue_maxsize`（默认 1000）；队列满直接标记 failed；`FullQueue` 捕获 |

测试：**96 passed** → 提交 `1a2a7fd`

---

## 全量测试结果（2026-08-14）

```
96 passed in 8.34s
```

连续 3 次全部通过，无 flaky 测试。

---

## 后续可选方向

| 方向 | 优先级 | 说明 |
|---|---|---|
| Celery/RQ 异步队列 | 中 | 替代内存队列，支持 Redis 分布式任务调度 |
| RBAC 权限表 | 低 | 扩展为可配置角色权限矩阵 |
| WebSocket 实时推送 | 中 | 替代前端轮询，实时推送运行状态变更 |
| Docker 多阶段构建优化 | 低 | 缩小镜像体积 |
| E2E Playwright 测试 | 低 | 覆盖前端完整交互链路 |
| 操作手册 & 演示视频 | 高 | 毕设答辩材料 |