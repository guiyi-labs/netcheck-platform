global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: netcheck-backend
    metrics_path: /metrics
    static_configs:
      - targets: ["netcheck-backend:8000"]
        labels:
          service: netcheck-backend